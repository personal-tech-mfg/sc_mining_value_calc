import 'package:flutter/material.dart';

class ValueDisplay extends StatelessWidget {
  const ValueDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Final Ore Value"),
      ),
    );
  }
}
